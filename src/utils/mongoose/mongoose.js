// import mongoose from 'mongoose';

export class Mongoose {
	// static connect = (options = { useNewUrlParser: true, useUnifiedTopology: true }) => {
	// 	const HOST = process.env.DB_HOST;
	// 	const NAME = process.env.DB_NAME;
	// 	const useNewUrlParser = 'useNewUrlParser' in options ? options.useNewUrlParser : true;
	// 	const useUnifiedTopology = 'useUnifiedTopology' in options ? options.useUnifiedTopology : true;
	// 	return mongoose
	// 		.connect(`mongodb://${HOST}:/${NAME}`, { useNewUrlParser, useUnifiedTopology })
	// 		.then(() => {
	// 			console.log('connected to mongoDB');
	// 		})
	// 		.catch((err) => {
	// 			console.log(err);
	// 		});
	// };
}
